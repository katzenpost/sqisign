name: ci

on:
  pull_request:

env:
  CARGO_TERM_COLOR: always

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: "1.94"
          components: rustfmt, clippy

      - uses: Swatinem/rust-cache@v2

      - name: Format
        run: cargo fmt --all --check

      - name: Clippy (deny warnings)
        run: cargo clippy --workspace --lib --tests -- -D warnings

      - name: Build
        run: cargo build --workspace --lib --tests

      - name: Test (workspace, includes the shake256 differential vectors)
        # We intentionally drop --benches from the target set. cargo test
        # treats criterion benches as test-mode smoke runs and the
        # criterion harness reports progress for every Fp primitive,
        # which adds minutes to every CI run and surfaces no
        # information the unit tests do not already cover. Benches
        # still build and run locally via `cargo bench`.
        run: cargo test --workspace --lib --tests

      - name: Verify-only crate stays signing-free
        # Katzenpost mix nodes pull only the verification path; building the
        # umbrella with default features must not drag in sqisign-sign.
        run: |
          cargo tree -p sqisign --edges normal | tee /tmp/tree.txt
          if grep -q '^.*sqisign-sign' /tmp/tree.txt; then
            echo "::error::sqisign-sign present in default (verify-only) tree"
            exit 1
          fi
